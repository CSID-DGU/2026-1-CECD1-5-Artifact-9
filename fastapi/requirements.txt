fastapi==0.115.5
uvicorn[standard]==0.32.1
# torch 와 torchvision 은 짝으로 움직인다 — torchvision 0.20.1 은 torch==2.5.1 을 못박아 요구한다.
# 한쪽만 올리면 pip 이 ResolutionImpossible 로 설치를 거부하므로 (Dependabot 이 실제로 그렇게 올렸다)
# 올릴 때는 둘을 함께, 호환되는 조합으로 올린다. 그리고 버전을 올린 뒤에는 실제 추론을 한 번 돌려
# 결과를 대조한다 — 가중치 파일이 같아도 연산 구현이 바뀌면 예측이 달라질 수 있다.
torch==2.5.1
torchvision==0.20.1
timm==1.0.11
Pillow==12.3.0
python-multipart==0.0.31
numpy
